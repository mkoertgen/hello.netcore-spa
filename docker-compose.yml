version: "3.7"

#---------------------------------------------------------------------------
services:
  angular:
    build:
      context: ./angular
    ports: ["8080:80"]

  react:
    build:
      context: ./react
    ports: ["8081:80"]

  vue:
    build:
      context: ./react
    ports: ["8082:80"]

  aurelia:
    build:
      context: ./react
    ports: ["8083:80"]
